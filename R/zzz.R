default_checks <- c(
    check_extra_rows_base,
    check_extra_rows_comp,
    check_extra_cols_base,
    check_extra_cols_comp,
    check_modes,
    check_class,
    check_attributes,
    check_values
)
diffdf_data <- diffMain$new()